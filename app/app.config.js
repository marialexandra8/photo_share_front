angular.module("app")
    .config(function ($locationProvider, $rootProvider) {
        $locationProvider.hashPrefix("#!");

        // $rootProvider.
        //     when("/")
        //TODO add routings
    });